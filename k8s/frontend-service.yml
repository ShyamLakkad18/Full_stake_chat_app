kind: Service
apiVersion: v1
metadata:
  name: frontend
  namespace: nginx
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31000
  type: NodePort
